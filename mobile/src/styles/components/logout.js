import { StyleSheet } from "react-native";

export default StyleSheet.create({
  logoutButton: {
    padding: 25,
    position: "absolute",
    right: 0,
    bottom: 0,
  },
  logoutButtonCtnt: {
    fontSize: 40,
    color: "red",
  }
});
